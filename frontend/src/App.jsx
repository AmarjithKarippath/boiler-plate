export default function App() {
  return (
    <main className="page">
      <p className="eyebrow">React + Docker Compose + nginx</p>
      <h1>Hello, World</h1>
      <p className="subtitle">
        This app is served through an nginx reverse proxy.
      </p>
    </main>
  );
}
